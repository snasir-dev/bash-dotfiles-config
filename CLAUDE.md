# CLAUDE.md

<!-- Claude Code reads CLAUDE.md and not AGENTS.md, so this file imports the real one. All project
     guidance belongs in AGENTS.md, where every agent sees it; add something here only if no other
     agent could act on it. Both files stay in context for the whole session, so keep them tight.

     Do not run /init or /import here: both append a flattened copy of AGENTS.md into this file and
     recreate the duplicate this replaced. A symlink would also work, but needs Developer Mode on
     Windows and breaks on a clone without core.symlinks. -->

@AGENTS.md
